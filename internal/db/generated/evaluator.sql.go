// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: evaluator.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const evaluateTickets = `-- name: EvaluateTickets :many
WITH stats AS (
    SELECT 
        booking_code_id,
        COUNT(*) as total_legs,
        COUNT(*) FILTER (WHERE status = 'WON')::int as won_legs,
        COUNT(*) FILTER (WHERE status = 'LOST')::int as lost_legs,
        COUNT(*) FILTER (WHERE status = 'PENDING')::int as pending_legs,
        COUNT(*) FILTER (WHERE status = 'VOID')::int as void_legs
    FROM booking_selections
    WHERE booking_code_id = ANY($1::uuid[])
    GROUP BY booking_code_id
),
updated_tickets AS (
    UPDATE booking_codes bc
    SET status = CASE 
        WHEN s.lost_legs > 0 THEN 'LOST'
        WHEN s.pending_legs = 0 AND s.lost_legs = 0 THEN 'WON'
        ELSE 'PENDING'
    END
    FROM stats s
    WHERE bc.id = s.booking_code_id 
    RETURNING bc.id, bc.status, bc.code
)
SELECT 
    ut.id as booking_code_id,
    ut.code as booking_code,
    ut.status as ticket_status,
    s.total_legs::int as total_legs,
    s.won_legs,
    s.lost_legs,
    s.pending_legs,
    s.void_legs,
    tkt.user_id,
    ud.fcm_token
FROM stats s
JOIN updated_tickets ut ON ut.id = s.booking_code_id
JOIN user_tickets tkt ON tkt.booking_code_id = s.booking_code_id
JOIN user_devices ud ON ud.user_id = tkt.user_id
`

type EvaluateTicketsRow struct {
	BookingCodeID uuid.UUID `json:"booking_code_id"`
	BookingCode   string    `json:"booking_code"`
	TicketStatus  string    `json:"ticket_status"`
	TotalLegs     int32     `json:"total_legs"`
	WonLegs       int32     `json:"won_legs"`
	LostLegs      int32     `json:"lost_legs"`
	PendingLegs   int32     `json:"pending_legs"`
	VoidLegs      int32     `json:"void_legs"`
	UserID        uuid.UUID `json:"user_id"`
	FcmToken      string    `json:"fcm_token"`
}

func (q *Queries) EvaluateTickets(ctx context.Context, bookingCodeIds []uuid.UUID) ([]EvaluateTicketsRow, error) {
	rows, err := q.db.Query(ctx, evaluateTickets, bookingCodeIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EvaluateTicketsRow{}
	for rows.Next() {
		var i EvaluateTicketsRow
		if err := rows.Scan(
			&i.BookingCodeID,
			&i.BookingCode,
			&i.TicketStatus,
			&i.TotalLegs,
			&i.WonLegs,
			&i.LostLegs,
			&i.PendingLegs,
			&i.VoidLegs,
			&i.UserID,
			&i.FcmToken,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
